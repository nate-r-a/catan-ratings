.col-md-6
  - @game.gameplays.each do |gp|
    %a{:href => player_path(gp.player.name.downcase)}
      .panel.panel-default.scorecard
        .panel-heading
          .row
            .col-md-9
              .pull-left
                = image_tag "sheep.png", class: "avatar"
                %h1.avatar-name
                  = gp.player.name
            .col-md-3  
              .pull-right
                = gp.score.round(0)