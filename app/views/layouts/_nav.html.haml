%nav.navbar.navbar-default.navbar-static-top
  .container-fluid
    .navbar-header
      %a.navbar-brand.logo{:href => "/"}
        = ["🐑","🌲","🌾","⛰️","🎲"].sample
      %a.navbar-brand.logo-text{:href => "/"}
        Federation Catan
    %ul.nav.navbar-nav
      %li.active
        %a{:href => "#"}
          = "Game #{params[:number]}"
      %li.dropdown
        %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
          Players
          %span.caret
        %ul.dropdown-menu{:role => "menu"}
          - Player.all.each do |p|
            %li= link_to p.name, player_path(p)
            %li.divider
      = form_tag search_path, :class => "navbar-form navbar-left", :method => :get do
        = text_field_tag :number, nil, :class => "form-control", :placeholder => "Game #"
            
            
            
            
            
            
      // %form.navbar-form.navbar-left
      //   .form-group
      //     %input.form-control{:type => "text", :placeholder => "Game #"}
      //       %button.btn.btn-default{:type => "submit"}